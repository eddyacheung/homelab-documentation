# Actual Budget auto-sync credentials
# Copy these values into Portainer stack environment variables or a local .env file.
# Never commit real passwords, encryption keys, or Sync IDs.

ACTUAL_SERVER_PASSWORD=replace-with-actual-server-password
ACTUAL_BUDGET_SYNC_IDS=replace-with-budget-sync-id
ACTUAL_ENCRYPTION_PASSWORD=
